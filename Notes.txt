For 74hc595,
RCLK=Latch
SRCLK=Clock
SER=Data

"library" method of directly compiling ibrary source files at compile time allows for changing clock frequencies between projects without needing to recompile libraries. Or rather, it just recompiles them for the needed clock speed when used. 
